apiVersion: v1
kind: Service
metadata:
  name: kube-api
  labels:
    app: words-api
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: kube-api
  selector:
    app: words-api
  clusterIP: None
